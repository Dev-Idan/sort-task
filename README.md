## Setup

Clone the repo and install the dependencies.

```bash
git clone https://github.com/Dev-Idan/sort-task.git
cd sort-task
```

```bash
npm install
```

## Starting

To run the file:

```bash
node index
```
